<nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand text-navbar" href="{{ path('homepage') }}">Mam-Saussan</a>
        <button class="navbar-toggler color" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon color"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav mx-auto">
                <li class="nav-item ml- 2 mr-3">
                    <a class="nav-link text-navbar" href=" {{ path('homepage') }} ">Accueil</a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link text-navbar" href="{{ path('equipe') }}">L'équipe</a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link text-navbar" href="{{path('presentation')}}">La M.A.M</a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link text-navbar" href="{{path('fonction')}}">Fonctionnement</a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link text-navbar" href="{{path('projet')}}">Projet</a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link text-navbar" href="{{path('contact')}}">Contact</a>
                </li>
                <li class="nav-item mr-3">
                        <a href="{{ path('comment_index') }}" class="nav-link text-navbar">tous les commentaires</a>
                </li>                           
            </ul>

            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link text-card dropdown-toggle"  data-toggle="dropdown" id="accountDropdown">
                           <span>
                            {{app.user.firstname}} {{app.user.lastname}}
                            </span>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdown">
                            <a href="{{path('article_index')}}" class="dropdown-item">les news</a>
                            <a href="{{path('image_index')}}" class="dropdown-item text-card">les photos</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{path('account_logout')}}" class="dropdown-item">Déconnexion</a>
                        </div> 
                    </li>
                {% else %}
                    <li class="nav-item mr-3">
                        <a href="{{path('account_login')}}" class="nav-link text-navbar">Connexion</a>
                    </li>
                {% endif %}                        
            </ul>   
        </div>
    </nav>
    <hr class="navbar-hr">
    {% for label, messages in app.flashes %}
        <div class="container mt-5">
            <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
            </div>
        </div>
    {% endfor %}